云端训练问题：无标注牙可能被当成全背景负样本训练
问题摘要
当前 supervised 训练集可能把没有手工 CEJ 标注的牙也纳入了训练。
这些牙的 H_GT.nii.gz 是全 0，如果仍然参与 DiceCELoss/BCE，模型会被训练成“这颗牙没有 CEJ”。这会直接压低无标注牙的预测热图，造成无标注牙在推理时 H_pred max 很低、H_pred >= 0.30 区域为空或只有很小一块。

这不是本机训练问题。本机只使用已训练模型做推理、可视化和分析；需要在云端训练 pipeline 中修改。

当前证据
当前增量数据 run_manual_inc_041055_001/processed 中共有 244 个 tooth ROI。
其中只有 188 颗牙有非空手工点/非空 H_GT。
还有 56 颗牙没有手工标注，H_GT.max == 0。
但 manual point 分支里的 supervised dataset 逻辑会遍历 processed_dir/*/tooth_*，直接读取：
A_t.nii.gz
T_t.nii.gz
H_GT.nii.gz
如果没有额外过滤，H_GT=0 的无标注牙也会进入训练 batch。
当前观察到的无标注低置信牙包括：

044-22: H_pred max=0.0012
044-12: H_pred max=0.0090
044-25: H_pred max=0.0107
051-18: H_pred max=0.0281
041-12: H_pred max=0.0337
044-24: H_pred max=0.0431
055-41: H_pred max=0.0566
051-28: H_pred max=0.1667
041-22: H_pred max=0.2411
041-14: H_pred max=0.2952
这些牙不应仅解释为模型漏检；如果训练时把无标注牙当全背景，会系统性鼓励模型在这类牙上输出低概率。

注意：不要把 041-055 的最终 train loss 高于 025-040 直接解释成“041-055 样本更难”。
这两个 run 的最终 loss 不能直接横向比较，尤其在 041-055 可能混入 H_GT=0 无标注牙时，loss 差异首先可能来自训练样本过滤/监督语义不同，而不是病例难度不同。

需要云端修改
1. supervised dataset 只训练有 CEJ 标注的牙
在 codex/manual-points-group-col 分支中检查 src/datasets/dataset.py 的 supervised item 构建逻辑。

当前风险逻辑类似：

for tdir in list_tooth_dirs(processed_dir):
    items.append({
        "image": ...,
        "mask": ...,
        "label": tdir / "H_GT.nii.gz",
    })
应改为只保留 H_GT 非空或 points.json 中该牙手工点非空的 tooth dir。

推荐判定：

points.json 存在且 points[tooth_id] 长度 > 0，或
H_GT.nii.gz 存在且 max(H_GT) > 0
无标注牙不应该作为全 0 label 进入 CEJ supervised loss。

2. 增加训练前统计日志
训练开始前打印并写入文件：

total_tooth_dirs
used_labeled_tooth_dirs
skipped_unlabeled_tooth_dirs
skipped 列表：case_id/tooth_id
每个牙类的 used/skipped 数量
这样可以避免以后 silently 把无标注牙当负样本。

3. 如果确实想使用无标注牙，应使用 ignore mask，而不是全背景 label
无标注牙可以用于半监督/一致性训练，但不能直接用 H_GT=0 参与 DiceCE/BCE。
如果要保留这些 ROI，必须给 loss 加 ignore mask，让未知区域不贡献 CEJ loss。

短期最安全做法：先过滤掉无标注牙，只训练有标注牙。

检查实施方法
A. 训练前做一次只读审计
在云端训练机先运行一个只读检查脚本，确认 processed 目录中到底有多少 tooth ROI、多少有标注、多少 H_GT=0。

示例脚本：

python - <<'PY'
import json
from pathlib import Path

import nibabel as nib
import numpy as np

processed = Path("/root/cej_runs/run_manual_inc_041055_001/processed")

def count_points(points_path, tooth_id):
    if not points_path.exists():
        return 0
    try:
        data = json.load(points_path.open())
    except Exception:
        return 0
    points = data.get("points") if isinstance(data, dict) else None
    if isinstance(points, dict):
        return len(points.get(str(int(tooth_id)), []) or [])
    if isinstance(points, list):
        return len(points)
    return 0

total = 0
manual = 0
hgt_nonzero = 0
unlabeled = []

for tdir in sorted(processed.glob("*/tooth_*")):
    total += 1
    case_id = tdir.parent.name
    tooth_id = tdir.name.split("_")[-1]
    n_points = count_points(tdir / "points.json", tooth_id)
    hgt_path = tdir / "H_GT.nii.gz"
    hgt_max = 0.0
    if hgt_path.exists():
        hgt = np.asarray(nib.load(str(hgt_path)).dataobj)
        hgt_max = float(hgt.max()) if hgt.size else 0.0
    has_label = n_points > 0 or hgt_max > 0
    manual += int(n_points > 0)
    hgt_nonzero += int(hgt_max > 0)
    if not has_label:
        unlabeled.append(f"{case_id}/{tdir.name}")

print("total_tooth_dirs=", total)
print("manual_point_teeth=", manual)
print("hgt_nonzero_teeth=", hgt_nonzero)
print("unlabeled_or_empty_hgt=", len(unlabeled))
print("first_unlabeled=", unlabeled[:30])
PY
对当前 041-055，期望看到：

total_tooth_dirs=244
hgt_nonzero_teeth=188
unlabeled_or_empty_hgt=56
如果训练 dataset 的 item 数仍然是 244，就说明无标注牙确实进入了 supervised 训练。

B. 在 dataset 构建处增加过滤和日志
检查 src/datasets/dataset.py 中 supervised item 构建函数，通常是 _build_supervised_items(...)。

建议增加一个 helper：

def _count_points(points_path, tooth_id):
    if not os.path.exists(points_path):
        return 0
    try:
        with open(points_path, "r", encoding="utf-8") as f:
            data = json.load(f)
    except Exception:
        return 0
    points = data.get("points") if isinstance(data, dict) else None
    if isinstance(points, dict):
        return len(points.get(str(int(tooth_id)), []) or [])
    if isinstance(points, list):
        return len(points)
    return 0


def _has_nonempty_hgt(label_path):
    if not os.path.exists(label_path):
        return False
    h = np.asarray(nib.load(label_path).dataobj)
    return bool(h.size and float(h.max()) > 0.0)


def _has_cej_label(tdir, tooth_id, label_path):
    points_path = os.path.join(tdir, "points.json")
    return _count_points(points_path, tooth_id) > 0 or _has_nonempty_hgt(label_path)
然后在 _build_supervised_items(...) 中过滤：

items = []
skipped = []
for tdir in list_tooth_dirs(processed_dir):
    tooth_id = os.path.basename(tdir).split("_")[-1]
    label_path = os.path.join(tdir, f"H_GT.{processed_format}")
    if not _has_cej_label(tdir, tooth_id, label_path):
        skipped.append(tdir)
        continue
    items.append({
        "image": os.path.join(tdir, f"A_t.{processed_format}"),
        "mask": os.path.join(tdir, f"T_t.{processed_format}"),
        "label": label_path,
        "tooth_dir": tdir,
    })
训练开始前必须打印：

supervised total_tooth_dirs=244
supervised used_labeled_tooth_dirs=188
supervised skipped_unlabeled_tooth_dirs=56
同时把 skipped 写到：

$SUP_RUN/outputs/train/skipped_unlabeled_teeth.txt
C. 加一个防回归测试
在云端 repo 增加一个 dataset 单测：

构造 2 个临时 tooth dir。
tooth_11 的 points.json 非空、H_GT 非空。
tooth_12 的 points.json 是 {"points": {"12": []}}，H_GT 全 0。
调用 supervised item 构建函数。
断言只返回 tooth_11，不会返回 tooth_12。
测试名建议：

test_supervised_dataset_skips_unlabeled_empty_hgt_teeth
这个测试必须在云端训练代码合并前通过。

验收标准
修改后重新跑当前 041-055 增量训练前，应看到类似：

total_tooth_dirs=244
used_labeled_tooth_dirs=188
skipped_unlabeled_tooth_dirs=56
训练后重点检查：

无标注低置信牙的 H_pred max 是否不再系统性接近 0。
044-22, 044-12, 044-25, 051-18, 041-12, 055-41, 051-28 等是否仍然完全无热图。
有标注 hard case 052-31/32/41/42/33 是否改善。
后续建议
这个修复优先级高于复杂 loss 改造。
如果无标注牙被当全背景训练，继续加 focal loss、骨架 loss 或表面约束之前，模型监督语义本身就是错的。应先修 dataset 过滤/ignore，再讨论结构 loss。