# Implementation Plan

1. Create project skeleton, config, schemas, scripts, and README.
2. Implement data IO, ROI crop/stitch, points handling, heatmap generation.
3. Implement model (3D U-Net), training, inference, postprocess, evaluation.
4. Implement visualization scripts (default interactive 3D viewer, optional 2D overlays) and unit tests.
5. Run smoke tests on TF_008 (supervised + unsupervised) without synthetic fallback.
